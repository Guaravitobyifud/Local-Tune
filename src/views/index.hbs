<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LocalTune</title>
    <link rel="stylesheet" href="estilo/style.css">
    <style>
        /* Estilo para o cabeçalho */
         *{
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            margin: 0;
        }
        .bo{
            color: #fff;
            text-decoration: none;
        }
        header {
            background-color: #46346e;
            color: #fff;
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .text_left {
            display: flex;
        }

        .text_left h3 {
            margin-right: 20px;
        }

        .buscar input {
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 5px;
            width: 200px;
            margin-right: 20px;
        }

        .buscar button {
            padding: 8px 20px;
            background-color: #46346e;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .buscar button:hover {
            background-color: #7f3eb4;
        }

        /* Estilo para a mensagem */
        .message {
            text-align: center;
            padding: 20px;
            background-color: #f2c8fa;
            color: #46346e;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <header>
        <div class="text_left">
            <h3><a class="bo" href="/login">Login</a></h3><!--se logado vira perfil-->
            <h3><a class="bo" href="/cadastro">Cadastro</a></h3><!--se logado vira perfil-->
        </div>
        <div class="buscar">
            <input type="text" placeholder="Pesquisar...">
            <button type="submit">Buscar</button>
        </div>
    </header>
    <div class="message">
        {{message}}
    </div>
</body>
</html>
